{
  "name": "cypress-test",
  "author": "Cypress DX Team",
  "license": "MIT",
  "scripts": {
    "build": "rollup -c",
    "start": "http-server",
    "test": "cypress run",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "cypress:record": "cypress run --record --key"
  },
  "dependencies": {
    "todomvc-app-css": "2.3.0",
    "todomvc-common": "1.0.5"
  },
  "devDependencies": {
    "@babel/core": "^7.13.14",
    "@babel/preset-env": "^7.13.12",
    "@rollup/plugin-babel": "5.3.0",
    "cypress": "^13.13.2",
    "cypress-file-upload": "^5.0.8",
    "http-server": "^14.1.1",
    "rollup": "2.44.0"
  },
  "description": "`yarn install` to install all the dependencies, `yarn build` to create `dist/bundle.js`, and `open index.html` to see it running in a browser.",
  "version": "1.0.0",
  "main": "cypress.config.js"
}
